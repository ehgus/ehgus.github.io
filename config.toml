# The URL the site will be built for
base_url = "https://ehgus.github.io"

# The site title and description; used in feeds by default.
title = "Dohyeon Lee's webspace"
description = "My little website"
default_language = "en"
theme = "tranquil"
output_dir = "public"
preserve_dotfiles_in_output = false

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true
minify_html = true
ignored_content = []

# When set to "true", a feed is automatically generated.
generate_feed = false
feed_filename = "atom.xml"
# When set to "true", files in the `static` directory are hard-linked. Useful for large
# static files. Note that for this to work, both `static` and the
# output directory need to be on the same filesystem. Note that the theme's `static`
# files are always copied, regardless of this setting.
hard_link_static = false

taxonomies = [
    { name = "tags"},
    { name = "categories"}
]
# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
highlight_code = true
extra_syntaxes_and_themes = ["highlight_themes"]
highlight_themes_css = [
    { theme = "tranquil-light", filename = "hl-light.css"},
    { theme = "tranquil-dark", filename = "hl-dark.css"},
]
highlight_theme = "css"
render_emoji = true

# Whether external links are to be opened in a new tab
# If this is true, a `rel="noopener"` will always automatically be added for security reasons
external_links_target_blank = true

# Whether to set rel="nofollow" for all external links
external_links_no_follow = true

# Whether to set rel="noreferrer" for all external links
external_links_no_referrer = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true

[link_checker]
# Skip link checking for external URLs that start with these prefixes
skip_prefixes = [
    "http://[2001:db8::]/",
]

# Skip anchor checking for external URLs that start with these prefixes
skip_anchor_prefixes = [
    "https://caniuse.com/",
]

# Treat internal link problems as either "error" or "warn", default is "error"
internal_level = "error"

# Treat external link problems as either "error" or "warn", default is "error"
external_level = "error"

[slugify]
paths = "on"
taxonomies = "on"
anchors = "on"
paths_keep_dates = false

[search]
# Whether to include the title of the page/section in the index
include_title = true
# Whether to include the description of the page/section in the index
include_description = false
# Whether to include the path of the page/section in the index
include_path = false
# Whether to include the rendered content of the page/section in the index
include_content = true
index_format = "elasticlunr_javascript"

[translations]

[languages]

[extra]
# Serene settings:
name = "Dohyeon Lee"    # Your name
id = "ehgus"    # Your id / username / handle
home = "home"   # Home name
bio = """
A programming student from the South Korea. Loves physics and programming.
""" # Your bio
avatar = "" # Your avatar
links = [                             # Your links
    { name = "GitHub", icon = "github", url = "https://github.com/ehgus" },
    { name = "Google Scholar", icon = "google_scholar", url = "https://scholar.google.com/citations?user=5OUiLvcAAAAJ&hl" }
]

nav_separator = " > "

display_id = false                    # Display your id on homepage
projects_page = true                  # Display projects page
blog_categorized = true               # Whether to categorize blog posts
blur_effect = true                    # Whether to turn on blur effect on navigation bar

toc = true                            # Whether to show Table-Of-Contents by default
comment = true                        # Whether to show giscus comment section by default, see https://giscus.app for more info
copy = true                           # Whether to add a copy button on code blocks by default
back_to_top = true                    # Whether to show back-to-top button by default

outdate_alert = false                 # Whether to show outdate alert by default
outdate_alert_days = 183              # How many days will a post be outdated by default
outdate_alert_text_before = "This article was last updated "
outdate_alert_text_after = " days ago and may be out of date."

call_to_action_footer_text = "Did you like this blogpost? Then consider subscribing via email or RSS. The email newsletter uses <a href=\"https://buttondown.email/refer/TDP\" target=\"_blank\"> Buttondown</a>, which does not track you."
call_to_action_footer = true

footer_copyright = ""
footer_credits = false                 # Whether to show "Powered by Zola and Serene" in footer

footer_privacy_text = "Privacy Policy"
footer_privacy = false